spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/regulatory_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  cache:
    type: simple

  mail:
    enabled: false
    host: smtp.gmail.com
    port: 587
    username: yourgmail@gmail.com
    password: your_app_password

server:
  port: 8080